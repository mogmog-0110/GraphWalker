# GraphWalker

数式を打ち込むと、その線がそのまま足場になる。あとは走って跳んで、ゴールの旗まで行く。
Siv3D の数式パーサで `F(x)` を解釈してグラフを引き、その曲線に当たり判定を持たせている。
たとえば `(2 * sin(x) + x) / 2` と入れると、うねりながら右上へ登る坂ができる。
`1.1` のような定数なら、ただの水平な橋になる。

きっかけは、数学が苦手な後輩だった。グラフを身近なものにしたくて、アクションの形にした。

C++ / Siv3D。`GraphWalker.sln` を Visual Studio で開いてビルドする。
